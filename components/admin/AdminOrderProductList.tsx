import { Typography } from "@/components";
import React from "react";

const AdminOrderProductList = () => {
  return (
    <div>
      <Typography className="text-gray-900 text-sm mt-2">
        No products found.
      </Typography>
    </div>
  );
};

export default AdminOrderProductList;
