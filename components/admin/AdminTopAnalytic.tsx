import React from "react";

type Props = {
  title: string;
  products?: ICreateProduct[];
  customers?: ICustomer[];
};

const AdminTopAnalytic = (props: Props) => {
  return <div>AdminTopAnalytic</div>;
};

export default AdminTopAnalytic;
